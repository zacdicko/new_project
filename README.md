A simple bash-script to help with setting up new design-projects.

How to use:
Run the script with ./folderstructure.sh (and add your project name as an argument.) <project_name>
e.g. $ ./folderstructure.sh <project_name>

